import { SelectionSortState } from "./selectionsortInterface";
import { sort } from "@/app/selectionsort"

function SelectionSortVisualizer({ state }: { state: SelectionSortState }) {



    return (
        {/* Render the array */ }
      {/* Show visual indicators */ }
    {/* Display current operation */ }

        <div>
            <h1>
              
            </h1>
        </div>
    )
}